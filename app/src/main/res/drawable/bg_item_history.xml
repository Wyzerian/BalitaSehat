<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background PUTIH -->
    <solid android:color="@android:color/white"/>

    <!-- Sudut membulat -->
    <corners android:radius="16dp"/>

    <!-- Shadow palsu (opsional, biar card naik) -->
    <padding
        android:left="2dp"
        android:top="2dp"
        android:right="2dp"
        android:bottom="2dp"/>
</shape>
